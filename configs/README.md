# Configuration Files

This directory contains configuration files defining:
- hyperparameters
- training settings
- random seeds
- model variants

Using configuration files improves reproducibility and separates
experimental settings from implementation code.

